const React = require('react');
const { useState, useRef, memo } = React;
require('./src/app.scss');

const slide = memo( () => {

    return (
        <>
            <div>
                
            </div>
        </>
    );
});

module.exports = slide;