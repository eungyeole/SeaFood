const React = require('react');
const { useState, useRef, memo } = React;
require('../app.scss');

const Slide = memo( () => {

    return (
        <>
            <div className="slide">
                
            </div>
        </>
    );
});

module.exports = Slide;