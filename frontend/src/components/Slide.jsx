const React = require('react');
const { useState, useRef, memo } = React;
require('../app.scss');

const Header = memo( () => {

    return (
        <>
            <div>
                
            </div>
        </>
    );
});

module.exports = Header;